#!/usr/local/bin/ysh

var dest = ARGV[0]

mkdir -p \
    "${dest}/usr/bin" \
    "${dest}/usr/share/doc/kiss"

cp -f kiss @[glob('contrib/*')] "${dest}/usr/bin"

var docs = @[glob('docs/site/*.txt')]
if (len(docs) > 0) {
    cp -f @docs "${dest}/usr/share/doc/kiss"
}

if test -d docs/site/wiki {
    cp -rf docs/site/wiki "${dest}/usr/share/doc/kiss"
}

rm -f \
    "${dest}/usr/share/doc/kiss/README.txt" \
    "${dest}/usr/share/doc/kiss/post.txt" \
    "${dest}/usr/share/doc/kiss/tidbits.txt"
