#!/usr/local/bin/ysh

var dest = ARGV[0]

var CC = ENV => get("CC", "cc")

make CC=$CC PREFIX=/usr
make PREFIX=/usr DESTDIR=$dest install

# Provide a 'ninja' symlink so cmake -GNinja works.
busybox ln -sf samu "${dest}/usr/bin/ninja"
