#!/usr/local/bin/ysh

var dest = ARGV[0]

./configure \
    --prefix=/usr \
    --sysconfdir=/etc

make
make DESTDIR=$dest install

busybox ln -s pkgconf "${dest}/usr/bin/pkg-config"

# Remove documentation.
busybox rm -rf "${dest}/usr/share/doc"
