#!/usr/local/bin/ysh

var dest = ARGV[0]

./configure \
    --prefix=/usr \
    --sysconfdir=/etc

make
make install

ln -s pkgconf "${dest}/usr/bin/pkg-config"

# Remove documentation.
rm -rf "${dest}/usr/share/doc"
