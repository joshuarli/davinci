#!/usr/local/bin/ysh

var dest = ARGV[0]

./configure \
    --prefix=/usr \
    ac_cv_func_malloc_0_nonnull=yes \
    ac_cv_func_realloc_0_nonnull=yes

make
make DESTDIR=$dest install

busybox ln -s flex "${dest}/usr/bin/lex"

busybox rm -rf "${dest}/usr/share/doc"
